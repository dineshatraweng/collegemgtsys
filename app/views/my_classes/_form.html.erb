<%= form_for(@my_class) do |f| %>
  <% if @my_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_class.errors.count, "error") %> prohibited this my_class from being saved:</h2>

      <ul>
      <% @my_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :size %><br />
    <%= f.text_field :size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
